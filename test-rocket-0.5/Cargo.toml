[package]
name = "test-rocket-0_5"
version = "0.12.0"
edition = "2021"
publish = false

[dev-dependencies]
askama = { workspace = true, features = ["derive"] }
askama_web = { workspace = true, features = ["derive", "rocket-0.5"] }

rocket = { version = "0.5.1", default-features = false }
